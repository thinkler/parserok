Insta Parser

= form_tag do
  = text_area_tag :dirty_urls
  = submit_tag "Process"

- if @clear_links
  %ul
    - @clear_links.each do |link|
      - if link
        %li= link
      - else
        %li None
